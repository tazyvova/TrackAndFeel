services:
  backend:
    volumes:
      - ..:/workspace:cached
    working_dir: /workspace
    # Keep the container running so we can attach to it
    command: /bin/sh -c "while sleep 1000; do :; done"
    # Ensure the dev container can see the host's docker daemon for act
    # (Though the feature should handle this, it's good to be explicit if needed)
